import StatusResponse from "../../../modules/shared_components/StatusResponse";

const Invite = () => {
  return (
    <StatusResponse response = 'attending' message = "Thank you for accepting the invite"/>
  )
 
};

export default Invite;
