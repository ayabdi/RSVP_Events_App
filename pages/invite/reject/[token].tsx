import StatusResponse from "../../../modules/shared_components/StatusResponse/StatusResponse";

const Invite = () => {
  return (
    <StatusResponse response = 'not attending' message = "Thank you for your response. Sorry you wont be able to make it."/>
  )
 
};

export default Invite;
